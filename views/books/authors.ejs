<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8">
    <title>Autores - Biblioteca</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    <style>
        body {
          font-family: 'Poppins', sans-serif;
          background-color: #0f111b;
          color: #f0e6d2;
        }
        .sidebar {
          background-color: #15161d;
          min-height: 100vh;
          padding-top: 1rem;
          color: #f0e6d2;
        }
        .brand {
          font-size: 1.5rem;
          font-weight: 700;
          padding-left: 1rem;
          margin-bottom: 2rem;
          color: #d4af37;
          display: flex;
          align-items: center;
          gap: 0.5rem;
        }
        .brand i {
          color: #d4af37;
          font-size: 1.8rem;
        }
        .nav-link {
          color: #f0e6d2;
          font-weight: 500;
        }
        .nav-link.active {
          background-color: #d4af37;
          color: #0f111b !important;
          border-radius: 5px;
        }
        .nav-link:hover {
          background-color: #d4af37;
          color: #0f111b !important;
          border-radius: 5px;
        }
        .user-info {
          color: #f0e6d2;
          margin-bottom: 2rem;
          padding-left: 1rem;
        }
        .user-info i {
          font-size: 2rem;
          margin-right: 0.5rem;
        }
        .user-info strong {
          display: block;
          font-weight: 600;
        }
        .user-info span {
          color: #d4af37;
          font-size: 0.9rem;
        }
        main.main-content {
          padding: 2rem;
          background-color: #1a1b23;
          border-radius: 8px;
          min-height: calc(100vh - 4rem);
          margin-left: 0;
        }
        .dashboard-header {
          background-color: #d4af37;
          color: #0f111b;
          padding: 1.5rem 2rem;
          border-radius: 0 0 1rem 1rem;
          margin-bottom: 2rem;
          font-weight: 700;
          display: flex;
          align-items: center;
          gap: 0.5rem;
        }
        .dashboard-header i {
          font-size: 1.5rem;
        }
        .alert-success {
          background-color: #3a6a34;
          color: #d4f0c0;
          border: none;
        }
        .alert-danger {
          background-color: #6a2f2f;
          color: #f0c0c0;
          border: none;
        }
        .card {
          background-color: #15161d;
          border: none;
          border-radius: 8px;
          box-shadow: 0 0 15px rgba(212,175,55,0.2);
        }
        .card-header {
          background-color: transparent;
          border-bottom: none;
          color: #d4af37;
          font-weight: 700;
          display: flex;
          justify-content: space-between;
          align-items: center;
        }
        .card-header i {
          margin-right: 0.5rem;
        }
        .btn-light {
          background-color: #d4af37;
          color: #0f111b;
          font-weight: 600;
          border: none;
          transition: background-color 0.3s ease;
        }
        .btn-light:hover {
          background-color: #b6932a;
          color: #0f111b;
        }
        table {
          width: 100%;
          border-collapse: collapse;
          color: #f0e6d2;
        }
        th, td {
          padding: 12px 15px;
          text-align: left;
          border-bottom: 1px solid #2f2f38;
        }
        thead {
          background-color: #d4af37;
          color: #0f111b;
        }
        tbody tr:hover {
          background-color: #2f2f38;
        }
        .badge.bg-success {
          background-color: #43a047 !important;
          color: #f0e6d2;
        }
        .badge.bg-danger {
          background-color: #e53935 !important;
          color: #f0e6d2;
        }
        .btn-success {
          background-color: #43a047;
          border: none;
          color: #f0e6d2;
        }
        .btn-success:hover {
          background-color: #357a38;
          color: #f0e6d2;
        }
        .btn-danger {
          background-color: #e53935;
          border: none;
          color: #f0e6d2;
        }
        .btn-danger:hover {
          background-color: #b32a26;
          color: #f0e6d2;
        }
        .action-buttons a {
          font-weight: 600;
        }
        footer.footer {
          margin-top: 3rem;
          padding: 1rem 0;
          background-color: #15161d;
          color: #d4af37;
          text-align: center;
          font-size: 0.9rem;
        }
        /* Mobile nav toggle styles */
        .mobile-nav-toggle {
          position: fixed;
          top: 1rem;
          left: 1rem;
          background: #d4af37;
          border: none;
          color: #0f111b;
          padding: 0.5rem 0.75rem;
          border-radius: 5px;
          z-index: 1050;
          display: none;
        }
        .mobile-nav-toggle i {
          font-size: 1.25rem;
        }
        @media (max-width: 767.98px) {
          .mobile-nav-toggle {
            display: block;
          }
          .sidebar {
            position: fixed;
            top: 0;
            left: -250px;
            width: 250px;
            height: 100%;
            z-index: 1040;
            transition: left 0.3s ease;
          }
          .sidebar.show {
            left: 0;
          }
          main.main-content {
            margin-left: 0 !important;
          }
          .sidebar-overlay {
            position: fixed;
            top: 0;
            left: 0;
            height: 100%;
            width: 100%;
            background: rgba(15,17,27,0.75);
            z-index: 1030;
            display: none;
          }
          .sidebar-overlay.show {
            display: block;
          }
        }
    </style>
</head>
<body>
    <!-- Mobile Navigation Toggle -->
    <button class="mobile-nav-toggle d-md-none" aria-label="Toggle navigation">
        <i class="fas fa-bars"></i>
    </button>

    <!-- Sidebar Overlay -->
    <div class="sidebar-overlay"></div>

    <div class="container-fluid">
        <div class="row">
            <!-- Sidebar -->
            <nav class="col-md-2 d-none d-md-block sidebar py-4" aria-label="Sidebar navigation">
                <div class="brand">
                    <i class="fas fa-book-reader"></i>
                    Biblioteca
                </div>
                <div class="user-info" role="region" aria-label="User information">
                    <i class="fa fa-user-circle"></i>
                    <strong>Edinson Berrio</strong>
                    <span>Administrador</span>
                </div>
                <ul class="nav flex-column" role="menu">
                    <li class="nav-item"><a class="nav-link" href="/"><i class="fa fa-home"></i> Dashboard</a></li>
                    <li class="nav-item"><a class="nav-link" href="#"><i class="fa fa-book"></i> Préstamos</a></li>
                    <li class="nav-item"><a class="nav-link active" href="/authors"><i class="fa fa-users"></i> Autores</a></li>
                    <li class="nav-item"><a class="nav-link" href="/editorials"><i class="fa fa-list-alt"></i> Editoriales</a></li>
                    <li class="nav-item"><a class="nav-link" href="/books"><i class="fa fa-book-open"></i> Libros</a></li>
                    <li class="nav-item"><a class="nav-link" href="/categories"><i class="fa fa-cogs"></i> Categorías</a></li>
                </ul>
            </nav>

            <!-- Main Content -->
            <main class="col-md-10 ms-sm-auto main-content" role="main" tabindex="-1">
                <div class="dashboard-header">
                    <i class="fa fa-users"></i>
                    <h2 class="mb-0">Gestión de Autores</h2>
                </div>

                <% if (messages.success) { %>
                <div class="alert alert-success alert-dismissible fade show" role="alert">
                    <i class="fas fa-check-circle me-2"></i><%- messages.success %>
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Cerrar"></button>
                </div>
                <% } %>
                <% if (messages.error) { %>
                <div class="alert alert-danger alert-dismissible fade show" role="alert">
                    <i class="fas fa-exclamation-circle me-2"></i><%- messages.error %>
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Cerrar"></button>
                </div>
                <% } %>

                <div class="card">
                    <div class="card-header">
                        <h5 class="mb-0"><i class="fas fa-users me-2"></i>Lista de Autores</h5>
                        <div>
                            <a href="/authors/add" class="btn btn-light btn-sm">
                                <i class="fas fa-plus me-2"></i>Agregar Autor
                            </a>
                        </div>
                    </div>
                    <div class="card-body">
                        <% if(data.length) { %>
                        <div class="table-responsive">
                            <table id="authorsTable" class="table table-hover">
                                <thead>
                                    <tr>
                                        <th scope="col">#</th>
                                        <th scope="col">Nombre</th>
                                        <th scope="col">Estado</th>
                                        <th scope="col" class="text-center">Acciones</th>
                                    </tr>
                                </thead>
                                <tbody>
                                <% for(var i = 0; i< data.length; i++) { %>
                                    <tr>
                                        <th scope="row"><%= (i+1) %></th>
                                        <td><%= data[i].name%></td>
                                        <td>
                                            <span class="badge <%= data[i].state === 1 ? 'bg-success' : 'bg-danger' %>">
                                                <%= data[i].state === 1 ? 'Activo' : 'Inactivo' %>
                                            </span>
                                        </td>
                                        <td class="text-center">
                                            <div class="action-buttons">
                                                <a class="btn btn-success btn-sm" href="/authors/edit/<%=data[i].id_author%>">
                                                    <i class="fas fa-pen-to-square me-2"></i>Editar
                                                </a>
                                                <a class="btn btn-danger btn-sm ms-2" href="/authors/delete/<%=data[i].id_author%>" onclick="return confirm('¿Está seguro de eliminar este autor?');">
                                                    <i class="fas fa-trash me-2"></i>Eliminar
                                                </a>
                                            </div>
                                        </td>
                                    </tr>
                                <% } %>
                                </tbody>
                            </table>
                        </div>
                        <% } else { %>
                            <p>No hay autores registrados.</p>
                        <% } %>
                    </div>
                </div>

                <footer class="footer mt-auto">
                    &copy; 2025 Biblioteca - Todos los derechos reservados.
                </footer>
            </main>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script>
      // Toggle sidebar on mobile
      const toggleBtn = document.querySelector('.mobile-nav-toggle');
      const sidebar = document.querySelector('.sidebar');
      const overlay = document.querySelector('.sidebar-overlay');
      
      toggleBtn.addEventListener('click', () => {
        sidebar.classList.toggle('show');
        overlay.classList.toggle('show');
      });
      overlay.addEventListener('click', () => {
        sidebar.classList.remove('show');
        overlay.classList.remove('show');
      });
    </script>
</body>
</html>
