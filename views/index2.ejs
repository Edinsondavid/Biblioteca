<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
  <title>Libros - Biblioteca</title>
  <!-- Favicon -->
  <link rel="icon" href="https://cdn-icons-png.flaticon.com/512/29/29302.png" type="image/png" />
  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&family=Playfair+Display:wght@400;500;600;700&display=swap" rel="stylesheet"/>
  <!-- Bootstrap 5 CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"/>
  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css"/>
  <style>
    body {
      font-family: 'Poppins', sans-serif;
      background-color: #0f111b;
      color: #f0e6d2;
    }
    .sidebar {
      background-color: #15161d;
      min-height: 100vh;
      padding-top: 1rem;
      color: #f0e6d2;
      position: fixed;
      left: 0;
      top: 0;
      width: 230px;
      z-index: 1000;
      display: flex;
      flex-direction: column;
    }
    .brand {
      font-size: 1.5rem;
      font-weight: 700;
      padding-left: 1rem;
      margin-bottom: 2rem;
      color: #d4af37;
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }
    .sidebar .nav-link,
    .sidebar .nav-link i {
      color: #f0e6d2;
      font-weight: 500;
      display: flex;
      align-items: center;
      gap: 0.7rem;
      padding-left: 0.5rem;
      transition: background 0.3s, color 0.3s;
    }
    .sidebar .nav-link.active,
    .sidebar .nav-link:hover {
      background-color: #d4af37;
      color: #0f111b !important;
      border-radius: 5px;
    }
    .sidebar .nav-link.active i,
    .sidebar .nav-link:hover i {
      color: #0f111b !important;
    }
    .user-info {
      color: #f0e6d2;
      margin-bottom: 2rem;
      text-align: center;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
    }
    .user-info i {
      font-size: 2rem;
      margin-bottom: 0.5rem;
      color: #d4af37;
    }
    .user-info strong {
      font-weight: 600;
      margin-bottom: 0;
    }
    .user-info span {
      color: #d4af37;
      font-size: 0.95rem;
    }
    .logout-link,
    .logout-link:visited {
      color: #ff5252 !important;
      font-weight: 500;
      border-radius: 5px;
      margin-top: 2rem;
      display: flex;
      align-items: center;
      gap: 0.7rem;
      padding: 0.8rem 1.2rem;
      transition: background 0.3s, color 0.3s;
      text-decoration: none;
      font-size: 1.08rem;
    }
    .logout-link:hover,
    .logout-link:focus {
      background-color: #d4af37;
      color: #15161d !important;
    }
    .logout-link:hover i,
    .logout-link:focus i {
      color: #15161d !important;
    }
    main {
      margin-left: 230px;
      width: calc(100% - 230px);
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      background: transparent;
      padding: 2rem 1rem;
      box-sizing: border-box;
    }
    .content-wrapper {
      width: 100%;
      max-width: 1100px;
      margin: 0 auto;
      display: flex;
      flex-direction: column;
      align-items: center;
    }
    .dashboard-header {
      background: #1a1b23;
      color: #f0e6d2;
      padding: 1.5rem 2rem;
      border-radius: 1rem;
      margin: 2rem auto;
      display: flex;
      align-items: center;
      gap: 1rem;
      justify-content: center;
      width: 80%;
      max-width: 900px;
      text-align: center;
      box-shadow: 0 2px 8px rgba(0,0,0,0.15);
    }
    .dashboard-header svg {
      fill: #d4af37;
    }
    .dashboard-header h2 {
      margin: 0;
      font-weight: 600;
    }
    .row.g-4.justify-content-center {
      justify-content: center !important;
      text-align: center;
    }
    .card-stat {
      border-radius: 1rem;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.10);
      display: flex;
      align-items: center;
      gap: 1rem;
      padding: 1.2rem 1.5rem;
      margin-bottom: 1.5rem;
      background: #1a1b23;
      color: #f0e6d2;
      font-weight: 600;
      justify-content: center;
      transition: box-shadow 0.25s, transform 0.18s, background 0.18s;
      cursor: pointer;
    }
    .card-stat:hover, .card-stat:active, .card-stat:focus {
      box-shadow: 0 8px 32px rgba(212, 175, 55, 0.15), 0 2px 8px rgba(0,0,0,0.08);
      transform: translateY(-4px) scale(1.03);
      background: #23243a;
    }
    .stat-title {
      font-size: 1.1rem;
    }
    .stat-value {
      font-size: 2rem;
      font-weight: bold;
    }
    .chart-container {
      display: flex;
      flex-direction: column;
      align-items: center;
      width: 100%;
      margin: 0 auto;
      max-width: 900px;
    }
    .chart-container h4 {
      margin-bottom: 1rem;
      text-align: center;
      width: 100%;
    }
    /* Aquí el CSS para mantener la gráfica proporcional y responsiva */
    #loansChart {
      display: block;
      margin: 0 auto;
      max-width: 800px;  /* ancho máximo fijo */
      width: 100%;       /* que ocupe todo el ancho disponible */
      height: 400px;     /* altura fija para mantener proporción */
      background: #23243a;
      border-radius: 8px;
      padding: 1rem;
    }
    .footer {
      background-color: #15161d;
      color: #d4af37;
      font-size: 0.9rem;
      margin-top: 3rem;
      padding: 1rem 0;
      text-align: center;
      border-top: 1px solid #d4af37;
      width: 100%;
    }
    @media (max-width: 991px) {
      .sidebar { min-height: auto; width: 100%; position: relative; }
      main { margin-left: 0; width: 100%; }
      .dashboard-header { flex-direction: column; width: 100%; max-width: 100%; }
      .chart-container { width: 100%; max-width: 100%; }
      /* Opcional: ajustar la altura en móvil para que no sea tan alta */
      #loansChart {
        height: 350px; /* un poco menos alto en móvil */
      }
    }
  </style>
</head>
<body>
  <div class="container-fluid">
    <div class="row g-0">
      <!-- Sidebar -->
      <nav class="sidebar">
        <div class="brand">
          <i class="fas fa-book-reader"></i> Biblioteca
        </div>
        <div class="user-info">
          <h5 style="font-weight:700; color:#fff; font-size:1.3rem; margin-bottom:0.2rem;">Edinson Berrio</h5>
          <small style="color:#d4af37; font-size:1rem; font-weight:500; letter-spacing:0.5px;">Administrador</small>
        </div>
        
        <ul class="nav flex-column px-3">
          <li class="nav-item">
            <a class="nav-link active" href="/index2">
              <i class="fas fa-home me-2"></i> Dashboard
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/books">
              <i class="fas fa-book me-2"></i> Libros
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/authors">
              <i class="fas fa-users me-2"></i> Autores
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/editorials">
              <i class="fas fa-building me-2"></i> Editoriales
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/loans">
              <i class="fas fa-handshake me-2"></i> Préstamos
            </a>
          </li>
          <a href="/logout" class="logout-link">
            <i class="fas fa-sign-out-alt"></i> Cerrar sesión
          </a>
        </ul>
      </nav>

      <!-- Main Content -->
      <main>
        <div class="content-wrapper">
          <div class="dashboard-header">
            <span class="dashboard-logo">
              <!-- Icono libro -->
              <svg width="48" height="48" viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg">
                <rect x="4" y="8" width="16" height="32" rx="3" fill="#d4af37" stroke="#6d4c41" stroke-width="2"/>
                <rect x="28" y="8" width="16" height="32" rx="3" fill="#d4af37" stroke="#6d4c41" stroke-width="2"/>
                <path d="M20 12C24 10 28 10 32 12" stroke="#6d4c41" stroke-width="2" fill="none"/>
                <path d="M20 36C24 38 28 38 32 36" stroke="#6d4c41" stroke-width="2" fill="none"/>
              </svg>
            </span>
            <h2><i class="fas fa-home me-2"></i>Inicio - Biblioteca</h2>
          </div>

          <div class="row g-4 justify-content-center">
            <!-- Libros Disponibles -->
            <div class="col-md-4">
              <div class="card-stat">
                <div class="icon"><i class="fas fa-book"></i></div>
                <div>
                  <div class="stat-title">Libros Disponibles</div>
                  <div class="stat-value">20</div>
                </div>
              </div>
            </div>
            <!-- Autores Registrados -->
            <div class="col-md-4">
              <div class="card-stat">
                <div class="icon"><i class="fas fa-users"></i></div>
                <div>
                  <div class="stat-title">Autores Registrados</div>
                  <div class="stat-value">20</div>
                </div>
              </div>
            </div>
            <!-- Préstamos Activos -->
            <div class="col-md-4">
              <div class="card-stat">
                <div class="icon"><i class="fas fa-handshake"></i></div>
                <div>
                  <div class="stat-title">Préstamos Activos</div>
                  <div class="stat-value">45</div>
                </div>
              </div>
            </div>
          </div>

          <!-- Gráfica centrada -->
          <div class="chart-container mt-5">
            <h4>Estadísticas Mensuales de Préstamos</h4>
            <canvas id="loansChart"></canvas>
          </div>
        </div>
      </main>
    </div>
  </div>

  <!-- Bootstrap Bundle with Popper -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  <!-- Chart.js para gráficos -->
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
  <script>
    const ctx = document.getElementById('loansChart').getContext('2d');
    const loansChart = new Chart(ctx, {
      type: 'bar',
      data: {
        labels: ['Enero', 'Febrero', 'Marzo', 'Abril', 'Mayo', 'Junio'],
        datasets: [{
          label: 'Préstamos',
          data: [12, 19, 8, 15, 10, 17],
          backgroundColor: '#d4af37',
          borderColor: '#6d4c41',
          borderWidth: 1
        }]
      },
      options: {
        responsive: true,
        maintainAspectRatio: true,
        scales: {
          y: {
            beginAtZero: true,
            ticks: { stepSize: 5 }
          }
        }
      }
    });
  </script>
</body>
</html>
